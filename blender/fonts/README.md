# fonts

3D の「雅」の一文字（提灯のテクスチャと、紋の立体文字）に和文フォントを使う。

```sh
./fetch.sh          # Noto Serif JP を取得
MIYABI_FONT=/path/to/YourMincho.ttf python build.py   # 差し替えたいとき
```

フォント本体はリポジトリに入れていない（13MB あるため）。
Noto Serif JP は SIL Open Font License 1.1。
